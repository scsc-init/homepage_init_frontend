import HeroMotionCompare from './HeroMotionCompare';

export const metadata = {
  title: '히어로 인트로 모션 비교',
  robots: { index: false, follow: false },
};

export default function HeroMotionTestPage() {
  return <HeroMotionCompare />;
}
